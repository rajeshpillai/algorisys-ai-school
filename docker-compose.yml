services:
  postgres:
    image: postgres:17
    container_name: ai-school-postgres
    ports:
      - "55433:5432"
    environment:
      POSTGRES_DB: ai_school_dev
      POSTGRES_USER: ai_school
      POSTGRES_PASSWORD: ai_school_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
